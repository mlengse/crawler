{
  "name": "url-to-markdown-converter",
  "version": "1.0.0",
  "description": "A powerful web-based tool that converts web pages to Markdown format",
  "private": false,
  "keywords": [
    "markdown",
    "converter",
    "url",
    "web-scraping",
    "react",
    "wasm",
    "rust",
    "batch-processing"
  ],
  "author": "mlengse",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mlengse/crawler.git"
  },
  "bugs": {
    "url": "https://github.com/mlengse/crawler/issues"
  },"dependencies": {
    "@testing-library/dom": "^10.4.0",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^13.5.0",
    "marked": "^15.0.12",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && cp public/sw.js build/",
    "build:vercel": "react-scripts build && cp public/sw.js build/",
    "test": "react-scripts test",
    "test:coverage": "react-scripts test --coverage --watchAll=false",
    "eject": "react-scripts eject",
    "deploy": "vercel --prod",
    "preview": "serve -s build",
    "lint": "eslint src --ext .js,.jsx",
    "lint:fix": "eslint src --ext .js,.jsx --fix",
    "postbuild": "cp public/sw.js build/"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
